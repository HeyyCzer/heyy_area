function sendBlockedWebhook(user_id, x, y, z)

end

-- type - como o blip foi removido
-- type = 1 : Blip removido pelo Policial
-- type = 2 : Blip removido por API - normalmente ao renascer
-- type = 3 : Blip removido pelo Policial deslogar
-- type = 4 : Blip removido por Admin
function sendUnblockedWebhook(user_id, x, y, z, type)

end
